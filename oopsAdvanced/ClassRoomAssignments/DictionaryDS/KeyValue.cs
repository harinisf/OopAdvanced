using System;
namespace DictionaryDS
{
    public class KeyValue<TKey,TValue>
    {
    public TKey Key { get; set; }
    public TValue Value{get; set; }
    }

    public bool LinearSearch(TKey key,out int position,out TValue Value)
    {
        position = -1;
        Value = default(TValue);
        for(int i =0;i <= count ; i++)
        {
            if(_array[i].Key.E)
        }
    }

    public class MyDctionary<TKey,TValue>
    {
        private int _count;
        private int _capacity;
        public int {get{return _count;}} 


        public KeyValue<TKey,TValue>[]_array;
    }

    public void Add(TKey key,TValue info)
    {
        if(_count == int_capacity)
        {
            GrowSize()
        }
        KeyValue<TKey key,Tvalue> data = new;
        data.Key = key;
        data.Value = info;

        _array[_countn
        _count++;

    })
}